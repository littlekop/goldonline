// shared palette — mirrors the CSS variables in app/globals.css, which
// GoldTracker.tsx and the theme toggle also read from, so the light/dark
// setting applies consistently to the articles pages too.
export const C = {
  paper: "var(--paper)",
  card: "var(--card)",
  cardSoft: "var(--card-soft)",
  ink: "var(--ink)",
  inkSoft: "var(--ink-soft)",
  inkFaint: "var(--ink-faint)",
  line: "var(--line)",
  red: "var(--red)",
  gold: "var(--gold)",
  profit: "var(--profit)",
  loss: "var(--loss)",
};
