# เว็บเช็คราคาทอง — สรุปสำหรับส่งต่อ Claude Code

## ภาพรวม
เว็บเช็คราคาทองภาษาไทย มีกระดานราคา (ทองแท่ง/ทองรูปพรรณ, THB/USD), เครื่องคำนวณต้นทุน-กำไรขาดทุนจากการซื้อทองหลายครั้ง, ราคาย้อนหลังที่ผู้ใช้บันทึกเอง, ระบบแชร์ผลลัพธ์เป็นภาพ, และพื้นที่โฆษณา/เนื้อหา SEO

**สถานะ:** ต้นแบบ (prototype) เสร็จแล้วในไฟล์ React artifact เดียว `gold-tracker.jsx` — พร้อมใช้เป็น reference ให้ Claude Code แปลงเป็นโปรเจกต์ Next.js จริง (ยังไม่ได้ deploy)

**ไฟล์ต้นแบบ:** แนบมาพร้อมกัน (`gold-tracker.jsx`) — เป็น single-file React component ใช้ recharts + lucide-react, สไตล์แบบมินิมอล แดง-ทอง (Chinese-inspired), ฟอนต์ Noto Serif Thai (หัวข้อ) + Sarabun (ตัวอักษร) + Space Mono (ตัวเลข)

---

## ฟีเจอร์ที่ทำเสร็จในต้นแบบ

1. **กระดานราคา** — ทองคำแท่ง / ทองรูปพรรณ (รับซื้อ-ขายออก), แปลง THB↔USD, คำนวณราคาต่อออนซ์ (1 บาท = 15.244 กรัม, 1 ออนซ์ = 31.1034768 กรัม)
2. **แก้ไขราคาตลาดเอง** — ราคาทั้งหมดเป็น manual input ในต้นแบบ (ยังไม่ได้ต่อ live API จริง)
3. **อัตราแลกเปลี่ยน USD/THB สด** — fetch จาก `https://api.frankfurter.app/latest?from=USD&to=THB` ตอนโหลดหน้า, fallback เป็นกรอกเองถ้า fetch ไม่สำเร็จ
4. **วันที่/เวลาปัจจุบัน** — แสดงบน ticker bar ด้านบน
5. **เครื่องคำนวณพอร์ตทอง** — เพิ่ม/ลบรายการซื้อได้ไม่จำกัด แต่ละรายการมี: ประเภท (แท่ง/รูปพรรณ), น้ำหนัก (เลือกหน่วยบาท/สลึงได้), ราคา/บาท, **ค่ากำเหน็จ** (บวกเข้าต้นทุน ไม่ได้คืนตอนขาย)
6. **สรุปพอร์ต** — น้ำหนักรวม, ต้นทุนรวม, ต้นทุนเฉลี่ย/บาท, มูลค่าปัจจุบัน (เลือกอิงราคาไหนก็ได้จาก 4 ราคาในกระดาน), กำไร/ขาดทุนรวม + %
7. **กราฟกำไร/ขาดทุนรายรายการ** — bar chart (recharts) สีเขียว=กำไร เทา=ขาดทุน (ตกลงกันแล้วว่าแดง-ทองใช้เป็นธีมตกแต่งอย่างเดียว ไม่ผูกกับความหมายกำไรขาดทุน)
8. **ราคาย้อนหลัง (self-logged)** — ปุ่ม "บันทึกราคาวันนี้" เก็บ snapshot ราคา+อัตราแลกเปลี่ยนลง `window.storage` (ต่อผู้ใช้ ไม่แชร์), มี dropdown เลือกวันที่เคยบันทึกดู และปุ่มโหลดกลับมาใช้เป็นราคาปัจจุบัน — **ไม่ใช่** ฐานข้อมูลย้อนหลังจริงของสมาคมค้าทองคำ
9. **Badge เทียบราคากับ snapshot ล่าสุด** — % เปลี่ยนแปลงเทียบกับวันที่บันทึกไว้ล่าสุด (ไม่ใช่ real-time เทียบเมื่อวาน เพราะไม่มี auto-archive)
10. **แจ้งเตือนราคา** — ตั้งราคาเป้าหมาย ใช้ browser Notification API, **ทำงานเฉพาะตอนเปิดหน้าเว็บค้างไว้เท่านั้น** (ปิดแท็บ/ปิดจอแล้วไม่แจ้ง — ต้องมี backend ถึงจะแจ้งแบบปิดแอปได้ ดูหัวข้อ "งานที่เหลือ")
11. **แชร์ผลลัพธ์เป็นภาพ (infographic)** — วาดด้วย Canvas API ล้วน (ไม่พึ่ง library ภายนอก) เป็น PNG โหลดเก็บเครื่องได้ หรือใช้ Web Share API ยิง share sheet มือถือ (มี fallback ถ้าเบราว์เซอร์ไม่รองรับ)
12. **เนื้อหา SEO** — 3 หัวข้อความรู้แบบ accordion (ทองแท่ง vs รูปพรรณ, คำนวณเงินสุทธิ, หน่วยชั่งทอง)
13. **พื้นที่โฆษณา** — วาง placeholder ไว้ 4 จุด (บนสุด, ใต้กระดานราคา, เหนือกราฟ, ท้ายเพจ) — สไตล์เส้นประทอง กลืนกับธีม

---

## ข้อจำกัดของต้นแบบ (สิ่งที่ artifact ทำเองไม่ได้)

- ไม่มี live price จริง — ราคาทองเป็น manual input ทั้งหมด (อัตราแลกเปลี่ยนอย่างเดียวที่ดึงสดได้)
- ไม่มี backend/database — ข้อมูลย้อนหลัง+พอร์ตอยู่แค่ในเบราว์เซอร์ผู้ใช้คนเดียว ข้ามอุปกรณ์ไม่ได้
- แจ้งเตือนใช้ไม่ได้เมื่อปิดแอป/ปิดจอ
- ไม่มีระบบบัญชีผู้ใช้ / sync ข้ามอุปกรณ์
- ไม่ใช่ PWA ติดตั้งลงเครื่องไม่ได้ (ต้อง deploy จริงก่อนถึงจะใส่ manifest + service worker ได้)

---

## งานที่เหลือสำหรับ Claude Code (เรียงตามความสำคัญ)

### 1. ดึงราคาทองสดจริง
ตัวเลือกที่คุยกันไว้:
- **thai-gold-api (GitHub: max180643/thai-gold-api)** — โปรเจกต์ open source scrape จากสมาคมค้าทองคำให้แล้ว → self-host เป็น backend ควบคุมได้เต็มที่ (แนะนำเป็นตัวหลัก)
- **thaigold.info** — `http://www.thaigold.info/RealTimeDataV2/gtdata_.json` มี JSON พร้อมใช้ แต่เป็นบริการบุคคลที่สาม ไม่มี SLA → ใช้เป็น fallback
- **goldtraders.or.th** — ต้อง scrape เอง (ไม่มี public API, ไม่รองรับ CORS จาก browser ต้อง scrape ฝั่ง server เท่านั้น)
- **ราคาทองโลก (USD/oz):** GoldAPI.io / Metals-API / MetalpriceAPI (มี free tier)

**สถาปัตยกรรมที่แนะนำ:**
```
Cron job (ทุก 1-2 นาที) → ดึงราคา (thai-gold-api เป็นหลัก + thaigold.info fallback) 
                          → เก็บ DB/Redis cache
Frontend → เรียก API ของตัวเอง (ไม่เรียกแหล่งข้อมูลตรง) → แสดงผล + ใช้เป็นฐานสร้างสถิติย้อนหลังอัตโนมัติ
```
เก็บราคาทุกครั้งที่ cron รันไว้ในตาราง history ด้วย จะได้มีข้อมูลย้อนหลังจริง (ไม่ต้องพึ่งผู้ใช้กดบันทึกเองแบบต้นแบบ)

### 2. แจ้งเตือนราคาแบบทำงานได้แม้ปิดแอป
แนะนำเริ่มจาก **Telegram Bot** (ฟรี, setup เร็วสุด) แล้วค่อยเพิ่ม **LINE Messaging API** ทีหลัง (⚠️ LINE Notify ปิดบริการไปแล้วตั้งแต่ 31 มี.ค. 2568 ห้ามใช้ตัวนั้น) ต้องมี DB เก็บ target price ต่อผู้ใช้ + cron เช็คเงื่อนไขแล้วยิงข้อความ

### 3. ระบบบัญชี/sync ข้ามอุปกรณ์ (ถ้าต้องการ)
ตัวเลือกที่เบาสุด: ผูกกับ Google Drive `drive.appdata` scope (แพทเทิร์นเดียวกับที่เคยใช้ในโปรเจกต์ Cycle Diary) — ไม่ต้องเก็บ user data เองฝั่ง server

### 4. PWA
manifest.json + service worker หลัง deploy จริงบนโดเมนตัวเอง

### 5. โมเดลหารายได้ (ที่คุยไว้)
- Google AdSense — วางตามจุด placeholder ที่เตรียมไว้แล้ว
- Affiliate ร้านทองที่มีตัวตนจริง (ฮั่วเซ่งเฮง, YLG, GFC ฯลฯ ที่ซื้อ-ขายทองจริง)
- **⚠️ ห้ามทำ affiliate forex / เทรดทองแบบ CFD** — การโฆษณา/ชักชวนลงทุน forex เข้าข่ายผิด พ.ร.ก. การกู้ยืมเงินที่เป็นการฉ้อโกงประชาชน พ.ศ. 2527 (จำคุก 5-10 ปี) เพราะไทยไม่มีใบอนุญาตประกอบธุรกิจ forex เลย ถ้าจะรับ affiliate แพลตฟอร์มเทรดทองใดๆ ต้องเช็คใบอนุญาต ก.ล.ต. ก่อนทุกครั้ง ว่าเป็นการซื้อ-ขายทองจริงไม่ใช่สัญญาส่วนต่าง (CFD)
- แจ้งเตือนราคาแบบพรีเมียม, บัญชี Pro ไม่มีโฆษณา, sponsored content จากร้านทอง, ขาย data/API ราคาย้อนหลัง

### 6. SEO ต่อยอด
เนื้อหา 3 หัวข้อที่มีอยู่เป็นจุดเริ่มต้น ควรมี dynamic routes + meta tags + JSON-LD ตามแพทเทิร์นที่ใช้ในโปรเจกต์อื่น (บิลบอร์ดผี, โค้ดเด็ด)

---

## Data model (จากต้นแบบ อ้างอิงตอนออกแบบ DB schema)

```js
market = { barBuy, barSell, jewelryBuy, jewelrySell }  // THB ต่อบาท
exchangeRate = number // THB ต่อ 1 USD

entry = {
  id, label, type: 'bar' | 'jewelry',
  weight: number, unit: 'baht' | 'salung',
  price: number,        // THB ต่อบาท ตอนซื้อ
  makingCharge: number, // THB ค่ากำเหน็จ ต่อรายการ
}

historySnapshot = { date, market, exchangeRate, savedAt }
```

## หมายเหตุสไตล์ (ให้ Claude Code ทำต่อให้ตรงธีม)
สี: ครีมกระดาษ `#FAF6EE`, หมึกเข้ม `#2B2420`, แดงตราประทับ `#B23A2E`, ทอง `#A9821F`, ทองอ่อน `#F3EAD1`, กำไร `#3C6E47`, ขาดทุน/เทา `#6B6258` — ฟอนต์: Noto Serif Thai (หัวข้อ) / Sarabun (เนื้อหา) / Space Mono (ตัวเลข)
